let dogName = 'Silver';
let catName = 'Blues';
let turtleName = 'veryFunnyTurtleIndeed';

let petName;

// petName = dogName + catName;

// concatenation

petName = dogName + ' ' + 'and ' + catName;

let age = 22;
let name = 'Sarah';

let friend;

friend = 'My friend ' + name + ' is ' + age + ' years old.';

// concat method

petName = dogName.concat(' ', catName);

// append

let = 'Sara ';
let += 'Bolton';

console.log(let);

// escape character

let = "JS isn't that much hard to learn. It's very cool";

//string length

let = dogName.length;
let = turtleName.length;

// string to lowercase

let = turtleName.toLowerCase();

// string to uppercase

let = turtleName.toUpperCase();

// indexOf();

let = dogName.indexOf('S');

// substring();

let = catName.substring(0, 2);

// slice();

let = catName.slice(-5);

// split();

let skills = 'HTML, CSS, BOOTSTRAP4, JavaScript, Git, GitHub, Balsamiq';

let = skills.split(',');

// replace();

let = skills.replace('CSS', 'Saas');

console.log(let);
